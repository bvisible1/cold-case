# Railway provides the PORT env var at runtime; fall back to 8080 locally.
server.port=${PORT:8080}

# Anthropic config (set ANTHROPIC_API_KEY as an env var; never commit it)
anthropic.model=${ANTHROPIC_MODEL:claude-sonnet-4-6}
anthropic.maxTokens=${ANTHROPIC_MAX_TOKENS:1024}
